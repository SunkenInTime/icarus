cmake_minimum_required(VERSION 3.14)
project(compact_wall_profiles LANGUAGES CXX)
add_library(compact_wall_profiles SHARED profiles.cpp ../native_tactical_rays/reference_cast.cpp)
target_compile_features(compact_wall_profiles PRIVATE cxx_std_17)
if(MSVC)
  target_compile_options(compact_wall_profiles PRIVATE /O2 /fp:strict /EHsc)
else()
  target_compile_options(compact_wall_profiles PRIVATE -O2 -fno-fast-math -ffp-contract=off)
endif()
