cmake_minimum_required(VERSION 3.14)
project(prepared_receiver_shadow_audit LANGUAGES CXX)
add_library(prepared_receiver_shadow_audit SHARED prepared.cpp)
target_compile_features(prepared_receiver_shadow_audit PRIVATE cxx_std_17)
if(MSVC)
  target_compile_options(prepared_receiver_shadow_audit PRIVATE /O2 /fp:strict /EHsc)
else()
  target_compile_options(prepared_receiver_shadow_audit PRIVATE -O2 -fno-fast-math -ffp-contract=off)
endif()
