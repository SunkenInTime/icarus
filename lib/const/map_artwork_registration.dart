import 'dart:ui';

import 'package:icarus/const/maps.dart';

// Measured from the paired artwork. Tests bind these values to the actual
// SVG fill bounds and the source-registration catalogue for every map.
const mapDefenseArtworkOffsetSvg = <MapValue, Offset>{
  MapValue.abyss: Offset(-.4823, -.194),
  MapValue.ascent: Offset.zero,
  MapValue.bind: Offset(-.822, 0),
  MapValue.breeze: Offset(-.066, 0),
  MapValue.corrode: Offset(-.213, 0),
  MapValue.fracture: Offset(-.262, 0),
  MapValue.haven: Offset(-.009, 0),
  MapValue.icebox: Offset(.459, 0),
  MapValue.lotus: Offset(-.907, -.386),
  MapValue.pearl: Offset(-.3323, 0),
  MapValue.split: Offset(-.8238, 0),
  MapValue.summit: Offset(-.385, 0),
  MapValue.sunset: Offset(-.028, 0),
};
